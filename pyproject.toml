[project]
name = "binary-master"
version = "0.2.0"
requires-python = ">=3.14"
dependencies = [
    "pytest>=9.1.1",
]

[project.scripts]
binary-master = "binary_master.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
